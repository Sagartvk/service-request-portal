pipeline {
    agent any

    stages {
        stage('Clone Repo') {
            steps {
                echo 'Cloning code...'
            }
        }

        stage('Build') {
            steps {
                echo 'Build stage running...'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying application...'
            }
        }
    }
}
